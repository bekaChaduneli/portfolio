import { IMainProjectResponse } from "@/types/mainProjects";
import React from "react";

export default function MainProjects({
  data,
}: {
  data: IMainProjectResponse | undefiend;
}) {
  return <div></div>;
}
